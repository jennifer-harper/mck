{% extends "_layout.twig" %} 
{% set bodyAttributes ={ class: "about" } %} 
{% set docTitle = entry.title %} 
{% set metaDesc = entry.metaDescription %} 

{% set page = craft.entries().section('about').all() %} 

{% block content %} 
    {% for entry in page %} 
    <h1>{{entry.title}}</h1>
    {% endfor %}
{% endblock %}